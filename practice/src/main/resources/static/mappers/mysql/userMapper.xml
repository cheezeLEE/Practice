<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userMapper">
	<insert id="join" parameterType="com.practice.join.dao.UserDAO">
		INSERT INTO tbl_user(userId, userPw, userName, userEmail, auth)
		VALUES(#{userId}, #{userPw}, #{userName}, #{userEmail}, #{auth})
	</insert>
	
	<select id="login" parameterType="com.practice.join.dao.UserDAO" resultType="int">
		SELECT COUNT(1)
		FROM tbl_user
		WHERE userId = #{userId}
			AND userPw = #{userPw}
	</select>
</mapper>